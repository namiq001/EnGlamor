@model List<Icon>
@{
    int count = 0;
}
<!-- Bootstrap Dark Table -->
<div class="card">
    <div class="row">
        <div class="col-6">
            <h5 class="card-header">Icon Table</h5>
        </div>
        <div class="col-6">
            <a asp-action="Create"><button class="btn btn-info" style="margin-top: 16px;">Create Icon</button></a>
        </div>
    </div>


    <div class="table-responsive text-nowrap">
        <table class="table table-dark">
            <thead>
                <tr>
                    <th>№</th>
                    <th>Title</th>
                    <th>Image</th>
                    <th>Setting</th>
                </tr>
            </thead>
            <tbody class="table-border-bottom-0">
                @foreach (Icon item in Model)
                {
                    <tr>
                        <td><i class="fab fa-angular fa-lg text-danger me-3"></i> <strong>@(++count)</strong></td>
                        <td>@item.Title</td>
                        <td>
                            <img src="~/assets/img/features/@item.IconImageUrl" alt="">
                        </td>
                        <td>
                            <div class="dropdown">
                                <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
                                    <i class="bx bx-dots-vertical-rounded"></i>
                                </button>
                                <div class="dropdown-menu">
                                    <form class="dropdown-item" asp-action="Detail" asp-route-id="@item.Id"><i class="bx bx-edit-alt me-1"></i><button style="border: none; outline: none; background: none;">Detail</button></form>
                                    <form class="dropdown-item" asp-action="Edit" asp-route-id="@item.Id" method="get"><i class="bx bx-edit-alt me-1"></i> <button style="border: none; outline: none; background: none;">Edit</button></form>
                                    <form class="dropdown-item" asp-action="Delete" asp-route-id="@item.Id" method="post"><i class="bx bx-edit-alt me-1"></i> <button style="border: none; outline: none; background: none;">Delete</button></form>
                                </div>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
<!--/ Bootstrap Dark Table -->